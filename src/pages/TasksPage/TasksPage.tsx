import { MainContainer, TasksContainer } from "../../modules/containers"

export const TasksPage = () => {
    return (
        <MainContainer>
            <TasksContainer/>
        </MainContainer>
    )
}